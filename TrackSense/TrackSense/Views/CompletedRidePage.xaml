<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TrackSense.Views.CompletedRidePage"
             xmlns:model="clr-namespace:TrackSense.Models"
             xmlns:viewmodel="clr-namespace:TrackSense.ViewModels"
             x:DataType="viewmodel:CompletedRideViewModel"
             Title="{Binding CompletedRide.CompletedRideId}">
    <Grid RowDefinitions="Auto,*,*,Auto"
          Margin="4">
            <Label Text="Itinéraire"
                   FontSize="Title"/>
            <!--<maps:Map x:Name="itineraryMap"
                      MapType="Street"
                      IsShowingUser="true"
                      Grid.Row="1"
                      Margin="10"
                      --><!--ItemsSource="{Binding Ride.Itinerary}"--><!--
                >
            </maps:Map>-->
        <!--<Label Text="Informations"
                   FontSize="Title"
                   Grid.Row="2"/>-->
        <Label Text="{Binding CompletedRide.Statistics.Distance}"
               Grid.Row="1"/>
        <Button x:Name="TrajetBtn"
                    Text="Favoris"
                    Grid.Row="3"/>
        </Grid>
</ContentPage>